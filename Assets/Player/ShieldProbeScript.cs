using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ShieldProbeScript : MonoBehaviour {
    int myIndex = 0;

    void SetIndex(int newInd) { myIndex = newInd; }

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
